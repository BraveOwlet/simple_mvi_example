CREATE TABLE DogEntity (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    url TEXT NOT NULL
);

insert:
INSERT INTO DogEntity(url)
VALUES(?);

removeAll:
DELETE FROM DogEntity;

selectAll:
SELECT DogEntity.*
FROM DogEntity;