CREATE TABLE SharedEntity (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    info TEXT
);

insert:
INSERT INTO SharedEntity(info)
VALUES(?);

removeAll:
DELETE FROM SharedEntity;

selectAll:
SELECT SharedEntity.*
FROM SharedEntity;